add_library(${CRATE_NAME} SHARED IMPORTED GLOBAL)

set_property(TARGET ${CRATE_NAME} PROPERTY IMPORTED_LOCATION "${CMAKE_CURRENT_SOURCE_DIR}/rust.dll")

target_link_libraries(${PLUGIN_NAME} PRIVATE ${CRATE_NAME})